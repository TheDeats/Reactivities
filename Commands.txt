dotnet new sln
dotnet new webapi -n API -f net9.0 -controllers
dotnet new classlib -n Domain -f net9.0
dotnet new classlib -n Application -f net9.0
dotnet new classlib -n Persistence -f net9.0
dotnet sln add API
dotnet sln add Domain
dotnet sln add Application
dotnet sln add Persistence
npm create vite@latest
npm install
npm run dev
dotnet watch
code .
dotnet tool install --global dotnet-ef --version 9.0.0 (Already installed)
npm install -D vite-plugin-mkcert -> Creates a local Certificate Authority which creates locally trusted development certificates.